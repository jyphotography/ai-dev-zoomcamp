# AI Assistant Commands

These are the commands used by the AI assistant to scaffold the project.

Files created:
- `package.json` (root)
- `client/` - React + Vite app
- `server/` - Express + Socket.io server
